box: wercker/default
build:
  steps:
    - arjen/hugo-build@1.6.1:
      theme: nofancy
        flags: --buildDrafts=true
deploy:
  steps:
    - lukevivier/gh-pages@0.2.1:
        token: $GITHUB_TOKEN
        basedir: public
        
